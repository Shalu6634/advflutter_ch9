import 'package:flutter/material.dart';

class SearchApiScreen extends StatelessWidget {
  const SearchApiScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
